services:
  ids:
    build: .
    container_name: ai_ids_app
    ports:
      - "5050:5050"
    volumes:
      - ./data:/app/data
      - ./models:/app/models
    command: python main.py --no-browser --host 0.0.0.0
